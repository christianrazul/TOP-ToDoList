const addTaskForm = document.querySelector("[data-add-task-form]");
const taskList = document.querySelector("[data-task-list]");

addTaskForm.addEventListener("click", (element) => {
  element.preventDefault();
});
