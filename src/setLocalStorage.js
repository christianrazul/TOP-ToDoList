// Resets the items that was collected earlier from Local Storage
// Saves item to Local Storage

// export default function setItems(items) {
//   const itemsJSON = JSON.stringify(items);

//   localStorage.setItem("todo-item", itemsJSON);
// }
